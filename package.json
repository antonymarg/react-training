{
    "name": "react-training",
    "private": false,
    "version": "1.0.0",
    "scripts": {
      "stackblitz:start": "cd react-version && npm install && npm run dev"
    },
    "stackblitz": {
      "installDependencies": false,
      "startCommand": "cd react-version && npm i && npm run dev",
      "env": {
        "NODE_ENV": "development"
      },
      "compileTrigger": "auto"
    }
  }
  